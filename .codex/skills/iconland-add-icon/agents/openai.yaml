interface:
  display_name: "Iconland Add Icon"
  short_description: "Add one icon pair into iconland"
  default_prompt: "Add this icon pair to grauity using the iconland workflow. Run from repo root, initialize submodules first (or rely on the script's auto-init), use explicit icon Type and name (ask for them if missing), ignore uploaded filenames, and return the resulting git status."
