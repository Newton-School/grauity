interface:
  display_name: "Iconland Add Icon"
  short_description: "Add one icon pair into iconland"
  default_prompt: "Add this icon pair to grauity end-to-end: initialize submodule first, push iconland changes to main, run build-icons and lint, create a grauity PR, then post to Slack via Web API using only SLACK_BOT_TOKEN. Slack message must be rich_text with real mention for @core-dev and keep this exact message content: '@core-dev Please review and merge' and '<pr_link>'."
