import { IconItem, Meta } from '@storybook/addon-docs';
import _ from 'lodash';

import { Icon } from 'ui/elements/Icon';
import { TAG_ICONS } from 'ui/core';


<Meta title="Elements/Icon/Gallery"/>

# Iconography

<div>
    {_.orderBy(Object.keys(TAG_ICONS)).map((key) => {
        return (
            <div>
                <h2>{_.startCase(key)}</h2>
                {
                    _.orderBy(TAG_ICONS[key]).map((icon) => {
                        return (
                            <IconItem name={icon} title={"Click to Copy"}>
                            <div style={{color: '#FFFFFF'}}>
                                <Icon name={icon} color="inherit" />
                                </div>
                            </IconItem>
                        )
                    })
                }
            </div>
        );
    })}
</div>
